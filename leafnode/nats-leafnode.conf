# NATS Leaf Node Configuration
# Connects to Azure NATS server

# Local listening port
port: 4222

# Monitoring port
http_port: 8222

# Server name
server_name: "onprem-leaf-node"

# Leaf node configuration - connects to Azure
leafnodes {
    remotes = [
        {
            # Azure NATS server URL
            url: "nats://admin:Admin@2025@nats-storeone-6537.northeurope.azurecontainer.io:4222"
        }
    ]
}

# Local authorization (optional - for on-prem clients)
authorization {
    user: "local"
    password: "local123"
}
